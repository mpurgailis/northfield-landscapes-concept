const obs=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.15});document.querySelectorAll('.reveal').forEach(e=>obs.observe(e));const r=document.querySelector('.compare input'),c=document.querySelector('.compare');r?.addEventListener('input',()=>c.style.setProperty('--pos',r.value+'%'));document.querySelector('.menu')?.addEventListener('click',()=>document.querySelector('.site-head nav').style.display=document.querySelector('.site-head nav').style.display==='flex'?'none':'flex');
